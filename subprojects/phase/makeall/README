阶段1：解析依赖包，并下载以及错误收集 (resolvedep)

阶段2：验证模块信息，并检查以及错误收集 (verifymod)

阶段3：联合编译各个模块，将编译结果打包到classpath，并收集错误信息 (joincompile)

阶段4：将各个模块打包成jar，并整合classpath。或将所有模块打包到一个包中，以及收集错误信息 (makeall) #

------------------------------------------------------

'#' 结尾的阶段代表当前模块所负责的阶段